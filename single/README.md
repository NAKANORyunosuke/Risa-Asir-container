# single/

単一コンテナ版の補助スクリプトを置くディレクトリです.

- `build.sh`: イメージを build する
- `run.sh`: コンテナを起動し, `bash` に入る
- `delete.sh`: コンテナとイメージを削除する

代表的な使い方:

```bash
./single/build.sh
./single/run.sh
./single/delete.sh
```
